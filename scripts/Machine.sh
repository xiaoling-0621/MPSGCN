'''
model = RF
area = BOHAI
n_estimators=100,
max_depth=None,
min_samples_split=5,
min_samples_leaf=1,
max_features = "sqrt"

area = ECS
n_estimators=200,
max_depth=10,
min_samples_split=5,
min_samples_leaf=2,
max_features = 0.5

area = SCS
n_estimators=200,
max_depth=10,
min_samples_split=5,
min_samples_leaf=2,
max_features = 0.5
'''

'''
model = SVR
area = BOHAI
C=1,
epsilon=0.1,
kernel=‘linear’

area = ECS
C=10,
epsilon=0.1,
kernel=‘linear’

area = SCS
C=1,
epsilon=0.01,
kernel=‘linear’。
'''

'''
model = Ridge
alpha=10
'''

